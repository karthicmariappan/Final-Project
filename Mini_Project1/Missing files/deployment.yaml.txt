apiVersion: apps/v1
kind: Deployment
metadata:
  name: trend
  labels:
    app: trend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: trend
  template:
    metadata:
      labels:
        app: trend
    spec:
      containers:
        - name: trend
          image: 123456789012.dkr.ecr.ap-south-1.amazonaws.com/trend:latest  # replace with ECR URI
          ports:
            - containerPort: 80    # change to 3000 if your container listens on 3000
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 20
            periodSeconds: 20
